version: '3.8'

services:
  medremind-backend:
    build:
      context: ./springboot-backend
      dockerfile: Dockerfile
    container_name: medremind-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    restart: unless-stopped
